# auspost-shipping-and-tracking-api-mcp-server

The canonical MCP README for this package is maintained at the repository root:

- `../../README.md`

SDK documentation is in:

- `../../readme-sdk.me`

During publish, `prepack` copies the root `README.md` into this package so npm always uses the project-root README as the single source of truth.
